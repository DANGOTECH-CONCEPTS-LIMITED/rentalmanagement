services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: https://api.marpleproperties.com
    image: rental-frontend:latest
    container_name: rental-frontend
    restart: unless-stopped
    ports:
      - "8081:80"