************** building docker image ******************
docker build -t property-management-api:local .


************** running docker image *******************
docker run --rm -p 5030:8080 `
  -e ASPNETCORE_ENVIRONMENT=Production `
  -e "ConnectionStrings__DefaultConnection=server=host.docker.internal;port=3306;database=testpropertydbprod_docker;user=root;" `
  -v "${PWD}\API\uploads:/app/uploads" `
  -v "${PWD}\API\logs:/app/logs" `
  propertymgtapi:0.5